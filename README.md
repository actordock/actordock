# actordock

Hundreds of agents, a handful of sandbox Pods.

Actordock multiplexes idle agent sandboxes onto a small pool of warm Workers—suspend on idle, resume on demand—so one Pod serves many sessions without paying full cold-start cost each time.
