# v0.0.7

**Status:** planned

## Target

**Sandbox extras** — Connect, network config, snapshots list/create.

| Before | After |
|--------|-------|
| No connect/network/snapshot REST | E2B SDK calls succeed (minimal or stub semantics) |

## Goal

```python
s = Sandbox.create(template="base", secure=False)
# connect / network / snapshot SDK paths do not 404
s.kill()
```

## In scope

| Area | Deliverable |
|------|-------------|
| Platform | `POST /sandboxes/{id}/connect` |
| Platform | `PUT /sandboxes/{id}/network` (accept/store; enforce minimal) |
| Platform | `POST /sandboxes/{id}/snapshots`, `GET /snapshots` |
| E2E | Routes return expected status (create snapshot may be stub) |

## Out of scope

- Full snapshot restore pipeline
- Network enforcement in Router/envd
- Template build from snapshot

## API

| Method | Path |
|--------|------|
| POST | `/sandboxes/{sandboxID}/connect` |
| PUT | `/sandboxes/{sandboxID}/network` |
| POST | `/sandboxes/{sandboxID}/snapshots` |
| GET | `/snapshots` |

**REST parity:** +4 ops → 18/56 (32%). See [e2b-rest-parity.md](e2b-rest-parity.md).

## Depends on

[v0.0.6](v0.0.6.md)

## Done when

- [ ] All four routes implemented (stub OK where noted)
- [ ] `GET /snapshots` lists sandbox-created entries or empty list
- [ ] Tests cover happy path + 404 for missing sandbox
